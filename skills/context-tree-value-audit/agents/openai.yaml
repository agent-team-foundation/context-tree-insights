interface:
  display_name: "Context Tree Value Audit"
  short_description: "Audit frozen Task, Read, and Effect evidence"
  default_prompt: "Use $context-tree-value-audit to reconstruct and freeze Tasks before attributing Context Tree Reads and zero-or-more Effects, using the available authorized sample."
policy:
  allow_implicit_invocation: false
